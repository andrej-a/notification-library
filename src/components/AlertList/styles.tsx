import styled from 'styled-components';

export const ListWrapper = styled.div`
    position: absolute;
    top: 0;
    left: 0;
    z-index: 0;

    width: 100vw;
    height: 100vh;
    padding: 10px;
    pointer-events: none;
`;
